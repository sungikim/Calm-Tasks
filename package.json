{
  "name": "obsidian-calm-tasks",
  "version": "0.1.1",
  "private": true,
  "description": "A calm, native-feeling task workspace for Obsidian Markdown tasks.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": ["obsidian", "tasks", "calendar", "markdown"],
  "license": "SEE LICENSE IN LICENSE",
  "devDependencies": {
    "@types/node": "^22.10.2",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.24.2",
    "obsidian": "latest",
    "typescript": "^5.7.2"
  }
}
